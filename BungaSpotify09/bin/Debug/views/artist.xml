<?
#include('views/generic-header.xml'); 
if data then
else
end 
?>
<document>
	<section id="overview" title="Overview">
		<hbox padding="12,12,12,12" height="202">
			<img src="${data.artist.image}" width="192" height="192" />
			<text flex="1" width="120" height="120">${data.artist.biography}</text>
			<box height="180" flex="1"> 
				<text>Related artists</text>
				
			</box>
		</hbox>
		
		%-- Show albums and singles
		
		% if data.artist.albums then
		<hbox bgcolor="#888888" height="20" width="100%"><text>Albums</text></hbox>
		
		% for album in enum(data.artist.albums) do
		% make_album(album, 192)
		% end
		% end
		% if data.artist.singles then
		<hbox bgcolor="#888888" width="100%" height="20"><text>Singles</text></hbox>
		
		% for single in enum(data.artist.singles) do
		% make_album(single, 192)
		% end
		% end
	</section>
</document>